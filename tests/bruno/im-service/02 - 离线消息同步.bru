meta {
  name: 02 - 离线消息同步
  type: http
  seq: 2
}

get {
  url: {{gateway_url}}/im/api/v1/messages/sync
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("消息同步 - HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("消息同步 - code 200", function() {
    expect(res.getBody().code).to.equal(200);
  });
}
