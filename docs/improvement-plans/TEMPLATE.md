# Improvement Plan {{number}}：{{主题}}

> 使用方式：复制本模板创建 `improvement-plan-N.md`，替换占位内容，在 [方案索引](../优化与演进.md) 登记编号，并在 [tasks.md](../../tasks.md) 关联执行阶段。修订同一方案时更新版本和记录，新增独立目标时使用下一编号。

| 字段 | 内容 |
|---|---|
| 方案编号 | Improvement Plan {{number}} |
| 版本 | 0.1 |
| 状态 | 草案 / 待实施 / 实施中 / 已完成 / 暂缓（选择一项） |
| 创建 / 更新日期 | YYYY-MM-DD / YYYY-MM-DD |
| 基线 | commit、工作区差异、运行环境及证据链接 |
| 前置条件 | tasks.md 阶段或其他方案/工作包 |
| 关联优化项 | O 编号与来源文档 |

## 背景与证据

说明具体使用场景、当前行为、问题及影响，引用源码、故障记录或测量结果。区分已验证事实与待验证假设；缺少基线时写明补测方法。

## 目标与范围

| 目标 | 本方案负责的结果与边界 | 完成判据 |
|---|---|---|
| G1 | {{用户或系统将获得的具体行为}} | {{可验证结果}} |

说明涉及的服务、调用方和业务范围，以及与既有设计或其他方案的分工。将暂缓事项及启动条件记录在风险章节。

## 设计方案与取舍

按实际请求或数据流描述方案，需要时添加 Mermaid 时序图。说明组件职责、数据归属、事务与幂等边界、故障恢复和权限传播。

| 设计问题 | 选定方式及依据 | 代价 / 适用条件 |
|---|---|---|
| {{问题}} | {{选择及证据}} | {{复杂度、资源或兼容性影响}} |

涉及分布式能力时，明确实例划分、ID 与连接归属、消息和后台任务协调、全局限流、数据副本、故障演练及资源预算；具体机制按本方案范围展开。

## 分步实施与依赖

| 工作包 | 目标 / O 编号 | 前置依赖 | 服务与文件 | 实施步骤与交付物 | 规模估计及假设 |
|---|---|---|---|---|---|
| IP{{number}}-W1 | {{目标}} | {{条件}} | {{范围}} | {{可执行步骤}} | {{依据与不确定性}} |

说明必须串行的步骤、可独立推进的工作及每阶段可演示结果；涉及迁移的工作包与下一章节关联。

## 接口、数据与发布迁移

| 对象 | 当前状态 → 目标状态 | 迁移与兼容方式 | 验证与回退点 |
|---|---|---|---|
| {{API / 表 / 事件 / 配置 / 调用方}} | {{变化}} | {{升级顺序、旧数据与调用方适配}} | {{方法}} |

按实际需要说明数据库迁移、事件版本、API 兼容期、前端切换、部署次序与旧机制退出条件。没有变化的对象可合并说明。

## 验收与效果比较

| 场景 / 指标 | 改动前基线 | 目标 / 不变量 | 方法、环境与数据 | 实际结果与证据 |
|---|---|---|---|---|
| {{场景}} | {{实测或待测}} | {{判据}} | {{命令 / 脚本 / 环境}} | 待执行 |

包含适用的用户流程、权限、重复请求、进程中断和恢复场景。性能比较使用相同数据、资源与并发模型，记录吞吐、延迟及错误比例；目标值与实测结果分列。

## 风险、回滚与暂缓项

| 风险 / 暂缓项 | 触发条件 | 处理或回滚步骤 | 回滚后数据核对 / 恢复条件 |
|---|---|---|---|
| {{事项}} | {{条件}} | {{操作及依赖}} | {{验证}} |

区分可直接回滚的代码与需要补偿或恢复的数据，说明备份、检查点及恢复方案。暂缓项写清重新启动的依据。

## 完成条件与交接

- [ ] 目标和工作包均有验收证据，遗留事项已登记。
- [ ] 数据、接口、调用方和当前设计文档同步。
- [ ] 迁移及回滚按适用场景完成验证。
- [ ] tasks.md 与方案索引的状态、版本和执行记录同步。

交接时记录当前工作包、最后验证位置、剩余事项和下一条可执行动作。方案状态“待实施”表示前置条件已满足且步骤、验收、迁移和回滚已明确；完成状态以实际证据为准。

## 修订与执行记录

| 日期 | 版本 / 工作包 | 变更与决策依据 | commit / 验证证据 | 下一步 |
|---|---|---|---|---|
| YYYY-MM-DD | 0.1 | 创建草案 | {{来源}} | {{动作}} |
